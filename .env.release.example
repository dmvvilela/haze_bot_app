# Copy to .env.release (gitignored) and fill in.

# App Store Connect numeric app ID (from App Store Connect → App Information → Apple ID field)
ASC_APP_ID=6747320369

# asc auth profile name — whatever you passed to `asc auth login --name`
ASC_PROFILE="D-Fit"
